<div class="chat_menu">
  <h1>Chat rooms</h1>

  <p class="lead"><%= link_to 'New chat room', new_chat_room_path, class: 'btn btn-primary' %></p>

  <ul>
    <% @chat_rooms_names.each do |chat| %>
      <li><%= link_to link_to chat.title, chat %></li>
    <% end %>
  </ul>
</div>
